<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Для самой прекрасной</title>
    <style>
        body {
            margin: 0;
            overflow: hidden;
            background: linear-gradient(-45deg, #ff9a9e, #fad0c4, #fbc2eb, #a6c1ee);
            background-size: 400% 400%;
            animation: gradient 15s ease infinite;
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            font-family: 'Georgia', serif;
            color: #6d4c67;
            text-align: center;
        }

        @keyframes gradient {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }

        #container {
            z-index: 10;
            position: relative;
            padding: 40px;
            background-color: rgba(255, 255, 255, 0.9);
            border-radius: 25px;
            backdrop-filter: blur(10px);
            box-shadow: 0 15px 35px rgba(0, 0, 0, 0.1);
            max-width: 650px;
            border: 2px solid rgba(255, 182, 193, 0.3);
        }

        h1 {
            font-size: 2.8em;
            margin-bottom: 25px;
            color: #d46a8b;
            font-weight: normal;
            text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.1);
        }

        p {
            font-size: 1.3em;
            line-height: 1.7;
            margin-bottom: 25px;
            color: #7c6a7c;
        }

        #hearts {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
            z-index: 1;
        }

        .heart {
            position: absolute;
            bottom: -50px;
            font-size: 22px;
            animation: floatUp linear forwards;
            opacity: 0.8;
            color: #ff9a9e;
        }

        @keyframes floatUp {
            to {
                transform: translateY(-100vh) rotate(360deg);
                opacity: 0;
            }
        }

        #secretMessage {
            display: none;
            margin-top: 35px;
            font-size: 1.3em;
            padding: 20px;
            background: linear-gradient(45deg, #ff9a9e, #fad0c4);
            border-radius: 15px;
            animation: fadeIn 2s;
            color: white;
            text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.2);
        }

        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }

        button {
            background: linear-gradient(45deg, #ff9a9e, #fad0c4);
            color: white;
            border: none;
            padding: 15px 30px;
            font-size: 1.2em;
            border-radius: 50px;
            cursor: pointer;
            transition: all 0.3s ease;
            box-shadow: 0 6px 20px rgba(255, 154, 158, 0.4);
            font-family: 'Georgia', serif;
        }

        button:hover {
            transform: scale(1.05) translateY(-3px);
            box-shadow: 0 8px 25px rgba(255, 154, 158, 0.6);
        }

        .flowers {
            position: absolute;
            font-size: 24px;
            opacity: 0.7;
            z-index: 2;
        }
    </style>
</head>
<body>
    <div id="hearts"></div>

    <div id="container">
        <h1>🌸 С Днём Рождения, моя принцесса! 🌸</h1>
        <p>Моя самая прекрасная, нежная и удивительная девушка!</p>
        <p>В этот особенный день я хочу сказать тебе, как ты дорога мне. Твоя улыбка делает мой день ярче, а твоё присутствие в моей жизни наполняет её особым смыслом и теплом.</p>
<p>Ты — самое красивое, что случалось со мной. Каждый момент рядом с тобой — это волшебство. Я восхищаюсь твоей силой, нежностью и тем светом, который ты несёшь в этот мир.</p>
        <p>Пусть этот год подарит тебе море радости, исполнения всех желаний и только счастливых моментов. Я всегда буду рядом, чтобы поддерживать и любить тебя.</p>
        <p>Люблю тебя бесконечно! 💖</p>
        
        <button onclick="showSecretMessage()">Нажми меня, моя радость</button>
        
        <div id="secretMessage">
            <p>Ты делаешь мой мир совершенным. Спасибо, что ты есть! Каждый день с тобой — это подарок судьбы. 🦋✨</p>
            <p>С любовью, твоя [Ваше имя] 💕</p>
        </div>
    </div>

    <script>
        // Создание летающих сердечек
        function createHearts() {
            const heartsContainer = document.getElementById('hearts');
            const heartSymbols = ['💖', '💕', '🌸', '💐', '🦋', '✨', '💫'];
            
            setInterval(() => {
                const heart = document.createElement('div');
                heart.className = 'heart';
                heart.textContent = heartSymbols[Math.floor(Math.random() * heartSymbols.length)];
                heart.style.left = Math.random() * 100 + 'vw';
                heart.style.animationDuration = (Math.random() * 3 + 5) + 's';
                heartsContainer.appendChild(heart);
                
                setTimeout(() => {
                    heart.remove();
                }, 8000);
            }, 300);
        }

        // Показать секретное сообщение
        function showSecretMessage() {
            document.getElementById('secretMessage').style.display = 'block';
            // Воспроизведение приятного звука (опционально)
            const audio = new Audio('data:audio/wav;base64,UklGRigAAABXQVZFZm10IBAAAAABAAEARKwAAIhYAQACABAAZGF0YQQAAAAAAA==');
            audio.play().catch(e => console.log('Автовоспроизведение заблокировано'));
        }

        // Запуск анимации сердечек при загрузке
        window.onload = createHearts;
    </script>
</body>
</html>